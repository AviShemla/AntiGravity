# Remaining gates before any real S08 fit

1. Reconcile the exact S07 preregistered model/sampler payload serialization
   with this backend's numeric payloads. A hash mismatch fails closed.
2. Instantiate the now synthetic-contract-tested immutable input bundle from
   the canonical normalized-edge Turso SSOT under a current S07 proof, then
   independently read back all 474 targets and four leakage-free folds. The
   injection-only reader and exact 1,896-payload coverage/tamper/overlap
   contract are implemented and Linux-tested; no Turso access occurred.
3. Instantiate the implemented Linux dependency-lock contract as a reviewed
   immutable release artifact. The first governed four-chain rehearsal was
   stopped after exceeding its 900-second harness bound, so convergence remains
   unproved. That attempt exposed and repaired a harness lifecycle defect:
   client timeout had orphaned one exact fixture process group; the group was
   terminated and independent readback proved no workers or UUID roots remain.
   Any new rehearsal must use the repaired server-side TERM/KILL bound and must
   finish with accepted R-hat/ESS/BFMI/divergence/tree-depth evidence.
4. Promote the fixture-only resource/ingestion-aware filesystem scaffold into
   the separate real-fit runner: verify authorization, apply OS-enforced cgroup
   limits, add identity-safe resume, and quarantine every partial or failed
   posterior. Its low-priority pre/post ingestion gates and durable auditor are
   Linux-tested, but it contains no process launcher and cannot authorize real
   data.
5. Extend the independent fixture terminal auditor to real runs so it reconciles 474 targets × 4 folds,
   hashes every output, rechecks zero temporal overlap and diagnostics, and
   proves zero database/downstream effects.
6. Obtain the exact explicit single-run research-fit authorization. No real fit
   is permitted by this fixture runner.
7. Posterior Turso schema/writer, operational prediction evidence, ETF priors,
   recommendations, orders, and trading remain separately gated and absent.
