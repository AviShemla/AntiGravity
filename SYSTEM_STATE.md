# AntiGravity AI Brain (SYSTEM_STATE)
> **AI INSTRUCTION:** At the start of every session, you MUST read this file to synchronize your memory with the current systemic truth.

## 1. Absolute Rules
- **Rule 1:** NEVER use OneDrive paths. All code execution and data storage happens strictly locally in `C:\Users\AviShemla\AntiGravity`.
- **Rule 2:** The project uses the `PyTensor NUTS` sampler. DO NOT use `nutpie`. Nutpie is unstable on Windows and crashes the memory cache.
- **Rule 3:** The intraday execution engine (`intraday_tracker.py`) securely reads its pending orders from the SQLite `antigravity.db` (`pending_orders` table) generated by `virtual_broker.py`, entirely deprecating the legacy `Pending_Orders.json` file.
- **Rule 4:** `run_backtests.py` dynamically uses the Top 15 tickers per sector. It only simulates a 1-day delta append, so runtime remains incredibly fast.
- **Rule 5:** ALL scripts must import their paths from `config.py` which dynamically reads from `settings.json`. NO HARDCODED PATHS are allowed in `.py` files.
- **Rule 6:** NEVER send emails, alerts, or Executive Briefs to the Mobideo work email address under any circumstances. STRICTLY use the personal Gmail address (`avi.shemla@gmail.com`).
- **Rule 7:** NEVER estimate, guess, or project Time-to-Completion (ETA) for dynamic computational workloads. Status updates must strictly contain pure mathematical facts extracted from the system logs (e.g. CPU time, chain counts, exact line outputs). No guessing.
- **Rule 8:** ZERO-TOLERANCE FOR CONCURRENCY OVERWRITES. All scripts must utilize OS-level atomic file locks (`os.O_CREAT | os.O_EXCL`) to completely eliminate the possibility of Watchdog/Cron cross-process contamination. "Trained" QA Agents MUST routinely audit background scripts to guarantee no hardcoded tmp filenames overlap.

## 2. Master Schedule
- **1:00 AM:** `master_pipeline.py` executes automatically via Windows Task Scheduler.
- **1:00 AM - 2:00 AM:** Nightly Bayesian engine (`daily_pipeline.py` -> `export_bayesian_scorecard_formatted.py`) runs.
- **2:00 AM - 7:30 AM:** `run_backtests.py` triggers automatically to update the Olympic Shootout.
- **9:30 AM EST (Market Open):** `intraday_tracker.py` wakes up from sleep to execute live orders.

## 3. Pre-Flight Protocol
Before making any massive codebase modifications, or executing backtests, run `python preflight_check.py` to assert the environment is totally clean.

---
### ??? June 13, 2026 Update: Deep Learning Shadow Engine & SQLite Hardening
- **Architecture Upgrades**: Successfully integrated the PyTorch LSTM & Transformer Shadow models alongside the PyMC engine. The new daily_dl_inference.py autonomously generates unified Single Stock and ETF Macro scorecards.
- **Data Continuity**: Fully migrated all ledgers away from volatile Excel/OneDrive dependencies into the secure  ntigravity.db SQLite schema.
- **QA Verifications**: Hardened  irtual_broker.py against fatal YFinance date mismatches, fixed tz-naive pandas crashes in intraday_tracker.py, and verified the Marathon engine's dynamic  date_range catch-up mechanism to prevent redundant simulations.
- **Status**: 100% Green. Portfolios balanced. FastAPI Web Dashboard active.
- **Rule 2:** NEVER report ETAs based on estimates. Only report factual progress metrics generated from the live `progress.json` state. No guessing.
- **Rule 3 (The Vertical Traceability Law):** Any time a Backend Data Schema, Model Name, or Persona Identity is altered in the Python pipeline or SQLite database, you MUST aggressively search the `/frontend/` folder for the old terminology and synchronize the HTML/JS. You are strictly forbidden from leaving the UI orphaned from the backend. All ETAs must be mathematically derived from factual code logic (e.g., total universe size minus processed models).

### UPDATE [2026-06-25]: 5-Day Clean Run Milestone Achieved
All systemic errors, flatlines, and date desyncs have been eradicated. The Virtual Broker + Intraday Tracker combination is functioning at 100% precision. Passed all system_qa_auditor.py checks across Data Integrity, Mathematical Validation, Ledger Accuracy, and Graphical Continuity.
